#include "servidor.h"

void imprimeTijolo() {

	//for (y = 4; y < 7; y++)
	//	for (x = 6; x < COLUNAS - 6; x++) {
	//		gotoxy(x, y);
	//		_tprintf(TEXT("#"));
	//	}

	//for (y = 8; y < 11; y++)
	//	for (x = 6; x < COLUNAS - 6; x++) {
	//		gotoxy(x, y);
	//		_tprintf(TEXT("&"));
	//	}





}